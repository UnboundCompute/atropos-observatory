import Link from 'next/link';
import Search from '../components/Search';
import catalog from '../.generated/catalog.json';
import stats from '../.generated/stats.json';

const featured = ['javascript/child-process/global/exec', 'python/pickle/global/load', 'c/stdlib/global/system', 'javascript/shell-quote/global/quote'];
const entries = featured.map((key) => catalog.find((entry) => entry.slug === key)).filter(Boolean);
const roleCounts = ['sink', 'source', 'sanitizer', 'summary'].map((role) => ({ role, count: catalog.filter((entry) => entry.role === role).length }));

export default function Home() {
  return <main><section className="intro"><div className="intro-copy"><p className="edition">Atropos / maintained security semantics</p><h1>Understand what an API does to data.</h1><p className="lede">Atropos Observatory is a public reference for security-sensitive API behavior. Read the short, reviewed guidance when you need orientation; inspect the complete generated model when you need evidence.</p><div className="hero-search"><Search /></div></div><dl className="register"><div><dt>Version</dt><dd>{stats.version}</dd></div><div><dt>Model rows</dt><dd>{stats.total.toLocaleString()}</dd></div><div><dt>Symbols</dt><dd>{stats.symbols.toLocaleString()}</dd></div><div><dt>Languages</dt><dd>{stats.languages.length}</dd></div><div><dt>License</dt><dd>CC BY 4.0</dd></div></dl></section><section className="path-section"><div className="section-head"><div><h2>Choose a starting point</h2><p>Two layers, with different promises.</p></div></div><div className="path-grid"><Link href="/guide" className="path-card"><span className="path-kicker">01 / Field guide</span><h3>Learn the vocabulary</h3><p>Understand source, sink, sanitizer, provenance, and the limits of a model fact before you interpret a record.</p><span className="path-action">Read the guide →</span></Link><Link href="/browse" className="path-card"><span className="path-kicker">02 / Model reference</span><h3>Inspect the complete register</h3><p>Search and filter every generated Atropos fact by language, role, weakness, and access path.</p><span className="path-action">Open the register →</span></Link></div></section><section className="search-section"><div className="section-head"><div><h2>Browse by model role</h2><p>Useful when you already know the boundary you are looking for.</p></div><Link className="text-link" href="/browse">Open complete index</Link></div><div className="role-index">{roleCounts.map(({ role, count }) => <Link href={`/browse/${role}`} key={role}><span className={`role role-${role}`}>{role}</span><b>{count.toLocaleString()}</b></Link>)}</div></section><section className="reference-section"><div className="section-head"><div><h2>Reference entries</h2><p>Common boundaries worth recognizing on sight.</p></div></div><table className="entry-table"><thead><tr className="entry-head"><th scope="col">Symbol</th><th scope="col">Environment</th><th scope="col">Semantics</th><th scope="col">Access path</th></tr></thead><tbody>{entries.map((entry) => entry && <tr className="entry-row" key={entry.id}><td data-label="Symbol"><Link href={`/symbols/${entry.slug}`}><code>{entry.method}</code></Link></td><td data-label="Environment">{entry.language} / {entry.package || 'stdlib'}</td><td data-label="Semantics"><span className={`role role-${entry.role}`}>{entry.role}</span></td><td data-label="Access path"><code>{entry.access_path || entry.id}</code></td></tr>)}</tbody></table></section><section className="network"><h2>Continue the investigation</h2><div><p>Observatory describes the model boundary. <a href="https://trace.unboundcompute.com/">Casefiles</a> shows it inside real vulnerabilities. <a href="https://lachesis.unboundcompute.com/">Lachesis Explorer</a> follows it through a codebase.</p><a className="network-source" href="https://github.com/UnboundCompute/atropos">Inspect the source model</a></div></section></main>;
}
